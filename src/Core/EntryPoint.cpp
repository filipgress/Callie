#include <clpch.h>

#include <Core/Application.h>

/**
 * TODO:
 * - grid
 * - axis
 * - obj. file loading capabilities (quads...)
 * - direct lighting (color, position, angle)
**/

int main(const int argc, const char** argv){
    cl::Application app("Callie"); 
    app.Run();

    return 0;
}